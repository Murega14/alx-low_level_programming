#include "main.h"
/**
 * swap_int - a function that lets you swap values for 2 integers
 * @a: an input pointer character
 * @b: an input pointer character
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	int swap;

	*a = swap;
	*a = *b;
	swap = *b;
}
